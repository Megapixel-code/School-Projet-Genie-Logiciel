class Imperfect_maze extends Maze {
    // do the rest
    public Imperfect_maze(int x, int y, int seed, Node start_node, Node end_node){
        /* 
         * init the imperfect maze
         * same as the maze class but with a different seed
         */
        super(x, y, seed, start_node, end_node);
    }
}
